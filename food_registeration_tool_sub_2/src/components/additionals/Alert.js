// src/components/additionals/Alert.js
import React from 'react';

const Alert = ({ message }) => {
    return (
        <div className="alert">
            {message}
        </div>
    );
};

export default Alert;

