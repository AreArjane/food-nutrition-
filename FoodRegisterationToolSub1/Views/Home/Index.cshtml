/// <summary>
/// Razor view for the main page of the application, featuring a GIF background and a centered search overlay.
/// </summary>
/// <remarks>
/// This view is designed to provide users with an engaging landing page that includes a search functionality for foods or meals.
/// The page is styled with external CSS and functionality is extended with JavaScript for dynamic search results.
/// </remarks>
///
/// <dependencies>
/// - Layout: The view relies on a shared layout located at "~/Views/Shared/_Layout.cshtml".
/// - CSS: Styling is applied through the external stylesheet "~/css/home.css".
/// - JavaScript: Dynamic functionality is implemented using the script "~/js/search.js".
/// </dependencies>
///
/// <example>
/// Use this view as the main page of the application:
/// - Users can input a search query, and results will dynamically populate the results section.
/// - Example: Typing "Pizza" will return relevant meal information (via the linked JavaScript file).
/// </example>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "MainPage";
}
<link rel="stylesheet" href="~/css/home.css">
<div class="gif-wrapper">
    <!-- GIF Background -->
    <img src="https://res.cloudinary.com/dnwuimgi4/image/upload/v1731588454/index_ffcn0m.gif" alt="Animated Design" class="animated-gif">

    <!-- Overlay for Search Section in the Center of GIF -->
    <div class="search-overlay">
        
        <input type="text" id="query" class="search-input" placeholder="Enter food or meal name" aria-label="Search query" />
        <button id="searchButton" class="search-button" type="button">Search</button>
    </div>
</div>

<div id="results" class="results-section" style="overflow-x:auto;">
    <!-- JavaScript will populate search results here -->
</div>

@section Scripts {
    <script src="~/js/search.js"></script>
}
