/// <summary>
/// Razor view that displays an "Access Denied" message to users who attempt to view a restricted page.
/// </summary>
/// <remarks>
/// This view is designed to provide a user-friendly response when access to a specific resource or page is denied.
/// It uses embedded HTML and CSS for layout and styling, ensuring compatibility across browsers and devices.
/// </remarks>
///
/// <example>
/// Usage in a Razor-based ASP.NET Core application:
/// - This view could be rendered by an action in a controller that checks user permissions.
/// - Example: If a user is not authorized, the controller redirects to this view.
/// </example>
///
/// <dependencies>
/// This view assumes that `ViewData["Title"]` is set in the controller or layout to provide a page title.
/// </dependencies>
@{
    ViewData["Title"] = "Access Denied";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Access Denied</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px;
        }
        .access-denied-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 30px;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #d9534f;
        }
        p {
            font-size: 18px;
        }
        a {
            color: #0275d8;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="access-denied-container">
        <h1>Access Denied</h1>
        <p>You do not have permission to view this page.</p>
        <p>If you believe this is an error, please contact support or try logging in with a different account.</p>
        <a href="/">Return to Home</a>
    </div>
</body>
</html>
